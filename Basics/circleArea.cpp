#include<iostream>
using namespace std;

int main() {
    
    float r, Area;
    cout << "Enter the value of r : ";
    cin >> r;

    Area = 3.1425f * r * r; // formula of radius of a circle;
    cout << " The area of the circle is :" << Area ;
    
   return 0;
}